/**
 * Script configuration framework for persistent, user-editable settings. Provides
 * {@code ScriptConfig} as the base configuration class and {@code ConfigField} for
 * annotating configurable fields with metadata such as labels and default values.
 */
package com.botwithus.bot.api.config;
